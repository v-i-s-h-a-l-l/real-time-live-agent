# Render / production requirements — Ministros voice backend
# Keep this lean: avoid packages that force Rust source builds (pyrnnoise, etc.)
# RNNoise / resemblyzer are NOT required for the live pipeline.

--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.136.1
uvicorn[standard]>=0.46.0
python-dotenv>=1.2.2
httpx>=0.28.1
aiohttp>=3.13.5
loguru>=0.7.3
numpy<2.0
scipy>=1.17.1
torch
pipecat-ai[cartesia,cerebras,deepgram,groq,sarvam,silero,websocket]>=1.1.0
